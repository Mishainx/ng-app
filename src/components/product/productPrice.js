import { formatPriceToUSD } from "@/utils/stringsManager";

export default function ProductPrice({ price, discount,user }) {
    console.log(user)
  return (
<div className="product-price"></div>
  );
}
